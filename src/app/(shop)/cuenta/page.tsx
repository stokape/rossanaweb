import { ComingSoon } from "@/components/shop/ComingSoon";

// Cuenta de cliente (opcional, Sección 37) — pendiente.
export default function CuentaPage() {
  return (
    <ComingSoon
      title="Mi cuenta"
      note="Recuerda: crear una cuenta es opcional, nunca obligatorio para comprar."
    />
  );
}
