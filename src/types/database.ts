/**
 * Tipos generados desde el esquema de Supabase.
 *
 * PLACEHOLDER — reemplazar ejecutando, una vez creado el proyecto de
 * Supabase y aplicadas las migraciones de /supabase/migrations:
 *
 *   npx supabase gen types typescript --project-id <PROJECT_ID> > src/types/database.ts
 *
 * Mientras tanto se usa `any` acotado para no bloquear el desarrollo de
 * la UI. No usar este placeholder como referencia del modelo de datos:
 * la fuente de verdad son las migraciones SQL.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
